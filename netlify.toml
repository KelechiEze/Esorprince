[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Handle client-side routing - important for React Router if you use it
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200